# Core web framework and WSGI serving
Flask==3.1.0
flask-cors==5.0.1
gunicorn==23.0.0

# Data validation and environment handling
# NOTE: Keep pydantic flexible for Python 3.14 so pip can resolve
# a wheel-supported build (older strict pins may trigger Rust source builds).
SQLAlchemy==2.0.38
psycopg2-binary==2.9.11,<3
pydantic>=2.11.7,<3
email-validator==2.2.0
python-dotenv==1.0.1

# Supabase database client
supabase==2.15.0

# Outbound integrations/utilities
requests==2.32.3
twilio==9.4.6
firebase-admin==6.7.0

# Testing
pytest==8.3.5

# Scheduling
APScheduler==3.10.4
